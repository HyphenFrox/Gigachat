fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
# `cryptography` is the only non-stdlib piece of the Ed25519 verify
# path. We pin to a major-version floor rather than an exact pin so
# the rendezvous can be redeployed against a newer wheel without a
# code change. The Ed25519 API has been stable since 2.5.
cryptography>=44,<48
