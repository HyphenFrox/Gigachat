Use latest technologies.

Keep the code modular, improving code reusability and reducing overall amount of code

Use libraries where appropriate. Don't try to create everything from scratch.

The UI should be modern, responsive and consistent throughout the app. It should feel like a native mobile app.

The app should have good error handling. Errors should be displayed to the user via Sonner component of ShadCN UI

The code should be well documented including comments.

If you use CSS, use SCSS instead

Use Javascript instead of Typescript

Always use best practices for security of the code. After making changes to the code, at the end, always verify if there are any security vulnerabilities introduced.

Make sure the database or API is secure

Don't leave any useless code. Remove it.

After making changes, check if README.md file needs to be upadated.

The code must be efficient

Don't add any personal or sensitive stuff in commits

The app has been deployed so newer versions of the app should be able to migrate older versions of the app to the current one

Don't commit your temporary scripts since they won't be needed by the app